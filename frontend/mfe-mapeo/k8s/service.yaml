apiVersion: v1
kind: Service
metadata:
  name: mfe-mapeo
  namespace: plataforma-incendios
  labels:
    app: mfe-mapeo
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      name: http
  selector:
    app: mfe-mapeo
